<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Adway_project" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="android:fontFamily">@font/pretendardmedium</item>
        <item name="android:textColor">#000000</item>
    </style>

    <!-- Splash screen theme -->
    <style name="SplashTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Adway_project</item>
    </style>

    <!-- Theme that inherits from Base.Theme.Adway_project -->
    <style name="Theme.Adway_project" parent="Base.Theme.Adway_project" />
</resources>


